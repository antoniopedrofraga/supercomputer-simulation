#ifndef STATETYPE_H //Define header if it's not defined
#define STATETYPE_H

//! StateType enum.
/*!
  Enum. Defining whether a state represents a starting or ending of a job.
*/
enum StateType { Start, End };

#endif